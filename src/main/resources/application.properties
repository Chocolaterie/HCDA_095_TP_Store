spring.application.name=eni-store

# Pour le developement : Mdp par défaut au lieu d'un mdp régénéré à chaque lancement pour gagner du temps
spring.security.user.password=password

# Profile (pour switch d'impl dao par exemple)
spring.profiles.active=prod,mondogb

# swagger-ui custom path
springdoc.swagger-ui.path=/swagger-ui.html

# Param bdd (SQL)
spring.datasource.url=jdbc:mysql://localhost:3306/db_articles?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Pa$$w0rd

# Param mongodb (NoSQL)
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=db_article
spring.data.mongodb.auto-index-creation=true

# Recreer le schema
spring.jpa.hibernate.ddl-auto=create
# -----------------
# On ne recreer le schema mais on inster les données
# spring.jpa.hibernate.ddl-auto=none
# spring.jpa.defer-datasource-initialization=true
# spring.sql.init.mode=always
# -----------------
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true